spring:
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
#        name: provider-test.yaml
#        group: DEFAULT_GROUP

  application:
    name: consumer

server:
  port: 9088

config:
  address: 192.168.10.1
  port: 10909